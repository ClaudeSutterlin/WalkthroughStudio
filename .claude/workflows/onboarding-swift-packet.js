export const meta = {
  name: 'onboarding-swift-packet',
  description: 'Write the Swift half of the Research Packet contract (models, validator, importer, --validate-packet flag, two probes), then review by reading and fix',
  phases: [
    { title: 'Write', detail: 'two writers with a shared API spec' },
    { title: 'Review', detail: 'compile-plausibility read-through' },
    { title: 'Fix', detail: 'apply findings, update build log S2' },
  ],
}

const COMMON = `
CONTEXT. Repository: Walkthrough Studio, a macOS SwiftUI Swift Package at the current working directory (macOS 14 floor, Swift 5.10 tools, no third-party Swift dependencies). Feature: "Onboard to a Codebase"; plan in docs/onboarding/ARCHITECTURE.md (read sections 2.2, 15.1 to 15.5, 15.6) and the contract in docs/onboarding/PACKET.md (read all of it) with the normative JSON Schemas in .claude/skills/onboarding-research/schema/*.json and the reference validator .claude/skills/onboarding-research/scripts/validate_packet.py (read it fully: the Swift validator mirrors its rules and message style). THERE IS NO SWIFT COMPILER HERE: write conservative, plain Swift you are confident compiles on Xcode 26 / macOS 14 SDK; mirror the style of the existing Onboarding code (read Sources/WalkthroughStudio/Onboarding/Package/Anchor.swift, OnboardingModels.swift, PackageStore.swift, GitRunner.swift, and Sources/WalkthroughStudio/SelfTestOnboarding.swift, SelfTestOnboarding+Package.swift for the probe pattern and OnboardingJSON helpers). Do not modify files you do not own. Do not run git commands that modify the repository.

SHARED API (both writers code against exactly this; writer D1 defines it, writer D2 uses it):
  // Sources/WalkthroughStudio/Onboarding/Packet/PacketModels.swift
  struct PacketManifest: Codable      // packet.json: version, producer{name, version, model?, startedAt, finishedAt, notes?}, repo{url?, headSHA, defaultBranch?, localPath?, name?}, scope (String), summary, counts{facts, verified, refuted, unknown?, traces, unreadDirs}, briefing?
  struct PacketInventory: Codable     // inventory.json (topLevel entries: path, files, bytes?, languages?, generated, reason?; entryPoints: anchor, kind, note?; manifests/ci/infra/docs: [String])
  struct PacketHistory: Codable       // history.json (authors, hotspots, ownership{dir, commits?, authors[{name, share, commits?}], busFactor}, stale, parallel{a,b,reason}, messageKeywords [String:Int], notableCommits{anchor, subject?, author?, date?, why})
  struct PacketDependencies: Codable  // dependencies.json; cves is either the string "unknown" or an array: model as enum PacketCVEs: Codable { case unknown; case list([PacketCVE]) }
  struct PacketFact: Codable          // one facts.jsonl line: id, kind, subject, claim, attributes ([String: JSONValue] or keep as a generic JSON blob), evidence[{anchor, excerpt?, note?}], confidence, producedBy, verdicts[{verifier, verdict, reason, evidence: [String]}], status, tags?
  struct PacketPaths: Codable         // paths.json: version, headSHA, candidates?, paths[{id, title, entry, rank, rationale, businessImpact?, factIds?, traced?}], untraced[{id, title?, reason}]
  struct PacketTrace: Codable         // traces/<id>.json: version, pathId, title, entry, scenario, hops[{n, anchor, callSite?, summary, factIds?}], concerns [String: PacketConcern{status, evidence, note?}] with the ten keys, scaresMe, factIds?
  struct PacketDecisions: Codable, struct PacketGlossary: Codable, struct PacketCoverage: Codable   // per schema
  struct ResearchPacket { let root: URL; let manifest: PacketManifest; let inventory: PacketInventory; let history: PacketHistory; let dependencies: PacketDependencies?; let facts: [PacketFact]; let paths: PacketPaths; let traces: [String: PacketTrace]; let decisions: PacketDecisions?; let glossary: PacketGlossary?; let coverage: PacketCoverage; let draftMarkdownFiles: [URL] }
  enum PacketReader { static func load(_ root: URL) throws -> ResearchPacket }   // throws StudioError("<file>: <reason>") on a missing required file or a decode failure; optional files may be absent
  All Codable types tolerate unknown keys (default synthesized decoding ignores unknown keys, so synthesized Codable is fine here; use explicit init(from:) only where a field is polymorphic like cves) and decode missing optionals as nil. Use OnboardingJSON.decoder()/encoder() from OnboardingModels.swift.

  // Sources/WalkthroughStudio/Onboarding/Packet/PacketValidator.swift
  struct PacketIssue: Codable { let where_: String; let message: String }   // encode the key as "where" via CodingKeys
  struct PacketValidationReport: Codable { var errors: [PacketIssue]; var warnings: [PacketIssue]; var stats: [String: Int]; var ok: Bool { errors.isEmpty } }
  struct PacketValidator { init(packet: ResearchPacket, repo: URL?, git: GitRunner)  ; func validate() async throws -> PacketValidationReport }
  // Sources/WalkthroughStudio/Onboarding/Packet/PacketImporter.swift
  enum PacketImporter { static func importPacket(at root: URL, into store: PackageStore, repo: URL?, git: GitRunner) async throws -> PacketValidationReport }  // validates, copies the whole packet directory into <store.root>/packet/ (replace), updates manifest.producer from packet.json (read/write via store.readManifest/writeManifest; create a minimal manifest if none exists), returns the report; throws StudioError when the report has errors (after writing nothing)

FIXTURE PACKET: a Research Packet for the fixture repository (scripts/make-fixture-repo.sh, head fb63e78744ce026d810547afbb62c0a08ce2040a) will be checked in at Sources/WalkthroughStudio/OnboardingResources/fixtures/fixture-repo.packet/ (Package.swift has .copy("OnboardingResources"), so at runtime it is Bundle.module.url(forResource: "fixture-repo.packet", withExtension: nil, subdirectory: "OnboardingResources/fixtures")). It may not exist in the tree yet while you write; code against the layout in PACKET.md.
`

const WRITE_SCHEMA = { type: 'object', properties: {
  filesWritten: { type: 'array', items: { type: 'string' } }, filesEdited: { type: 'array', items: { type: 'string' } },
  publicAPI: { type: 'string' }, uncertainties: { type: 'array', items: { type: 'string' } } }, required: ['filesWritten', 'filesEdited', 'publicAPI', 'uncertainties'] }

phase('Write')
const writers = [
  { key: 'D1-models', prompt: `${COMMON}
YOU ARE WRITER D1. You own Sources/WalkthroughStudio/Onboarding/Packet/PacketModels.swift (new) only. Implement every type in the SHARED API exactly, mirroring the ten JSON Schemas field by field (open each schema file). Add a small JSONValue enum (string, number, bool, null, array, object) for PacketFact.attributes and for any free-form object, Codable both ways. PacketReader.load reads packet.json, inventory.json, history.json, facts.jsonl (line by line, skipping blank lines, reporting the line number of a bad line), paths.json, coverage.json as required; dependencies.json, decisions.json, glossary.json and traces/*.json as optional; drafts/**/*.md listed into draftMarkdownFiles. Return the structured summary.` },
  { key: 'D2-validator-importer-probes', prompt: `${COMMON}
YOU ARE WRITER D2. You own Sources/WalkthroughStudio/Onboarding/Packet/PacketValidator.swift (new), PacketImporter.swift (new), Sources/WalkthroughStudio/SelfTestOnboarding+Packet.swift (new), and two small edits: add the "--validate-packet <packetDir> <repoDir>" flag to Sources/WalkthroughStudio/WalkthroughStudioApp.swift beside the other selftest flags (loads the packet with PacketReader, validates against the repo with a GitRunner, prints each error as "ERROR   <where>: <message>", each warning as "warning <where>: <message>", the stats as one line, then "PACKET OK: ..." or "PACKET INVALID: ..." exactly like validate_packet.py, exit 0 or 1), and append two probes to the runner list in Sources/WalkthroughStudio/SelfTestOnboarding.swift after gitRunnerProbe: packetValidateProbe and fixturePacketProbe (keep the existing entries byte-for-byte).
PacketValidator.validate() mirrors validate_packet.py in this order, with the same "where" strings and message wording where practical: (1) required files present (PacketReader already threw otherwise; still report headSHA mismatches between files); (2) every anchor parses with Anchor.parse and, for code: anchors, sha7 is a prefix of manifest.repo.headSHA; resolution against the repo when repo != nil using GitRunner.lineCount(sha:path:repo:) for files (line range inside the file), GitRunner.lsTree for directory anchors, and GitRunner.revParse or show for commit: anchors; cmd: anchors need commands/<unit>/<n>.txt under the packet root; fact: anchors need a known id; trace: anchors need the trace and hop; video:/doc:/diagram: anchors outside drafts produce a warning; (3) facts: evidence non-empty, refuted needs a refuting verdict with evidence, verified without a confirming verdict is a warning, dataEntity attributes pii/rows/retention missing is a warning; (4) paths and traces: each ranked path with traced != false needs a trace file, hop numbers 1..n, the ten concerns present, present/absent need evidence, factIds known; (5) coverage: every inventory directory listed, unknown directories are errors, level mapped/verified/traced needs facts > 0, unread needs filesRead == 0, filesRead <= files; (6) completeness gates: zero facts, a summary starting with "SUMMARY PENDING", or no directory at level mapped or above are errors; (7) counts in packet.json versus computed are warnings; (8) [[anchor]] citations inside drafts/*.md are parsed and checked. stats: facts, traces, paths, anchorsByKind (flatten as "anchors.code" etc.), coverage by level ("coverage.mapped" ...), factsByStatus ("status.verified" ...). Anchor resolution results must be cached per (sha, path) so a 200-fact packet does not spawn 500 git processes.
Probes (signature "static func <name>(_ ctx: OnboardingProbeContext) async throws" in extension SelfTest, print "selftest: <name> OK (<detail>)" or throw StudioError): packetValidateProbe locates the fixture packet in Bundle.module (fail with a clear message if absent), validates it against ctx.fixtureRepo and requires zero errors; then copies it into ctx.scratch/broken.packet, mutates facts.jsonl so the first fact's first evidence anchor points to a line range beyond the file (dangling), appends a fact with an empty evidence array (orphan), and deletes one traces/<id>.json whose path is ranked (missing trace); validates again and asserts at least three errors including one whose message contains "outside file" or "not found", one containing "no evidence", and one containing "has no traces/"; then runs PacketImporter.importPacket on the good packet into a PackageStore at ctx.scratch/import-pkg and asserts <root>/packet/facts.jsonl exists and the manifest producer name equals packet.json's producer name. fixturePacketProbe asserts the known findings structurally: a fact with kind hotspot whose subject contains "orders_repo.py"; a dataEntity fact whose subject contains "users" with attributes.pii == true; a fact whose claim mentions "002" and (migration or applied); a fact with subject containing "deploy.sh" whose claim contains "rollback"; a dependency entry named requests with version 2.19.0 whose cves is a non-empty list or eol equals "end-of-life"; every history.ownership entry has busFactor == 1; coverage directory "vendor/" has level unread or inventoried; some trace has concerns.idempotency.status == "absent" and its entry anchor path contains "orders_handler.py". Return the structured summary.` },
]
const written = (await parallel(writers.map(w => () => agent(w.prompt, { label: `write:${w.key}`, phase: 'Write', schema: WRITE_SCHEMA, effort: 'high' })))).filter(Boolean)
log(`writers done: ${written.length}/2`)

const REVIEW_SCHEMA = { type: 'object', properties: { findings: { type: 'array', items: { type: 'object', properties: { file: { type: 'string' }, line: { type: 'integer' }, severity: { type: 'string', enum: ['compile-error', 'runtime-bug', 'contract-mismatch', 'style'] }, problem: { type: 'string' }, fix: { type: 'string' } }, required: ['file', 'line', 'severity', 'problem', 'fix'] } }, verdict: { type: 'string' } }, required: ['findings', 'verdict'] }
phase('Review')
const review = await agent(`${COMMON}
YOU ARE THE REVIEWER, acting as the compiler and as the contract checker. Read the four new files and the two edited files (git status / git diff), plus Anchor.swift, GitRunner.swift, PackageStore.swift, OnboardingModels.swift and SelfTestOnboarding.swift which they call. Find anything that will not compile (wrong labels, missing imports, actor isolation: SelfTest is @MainActor and GitRunner calls are async; Codable synthesis versus explicit init; duplicate type names across files; access control), anything that diverges from validate_packet.py's rules or message wording in a way that would make the Swift and Python validators disagree on the fixture packet, and anything in the probes that cannot pass as written. Writer summaries and their own uncertainties: ${JSON.stringify(written)}
Read-only. Return the structured findings.`, { label: 'review', phase: 'Review', schema: REVIEW_SCHEMA, effort: 'high' })
log(`findings: ${review.findings.length}`)

phase('Fix')
const fixed = await agent(`${COMMON}
YOU ARE THE FIXER. Apply every compile-error, runtime-bug and contract-mismatch finding below after checking it against the file (skip and say so if a finding is wrong); apply style findings only when trivial. Re-read the four new files once more as a compiler would. Then in docs/onboarding/BUILD-LOG.md, under the existing "### S2:" entry, append to "Built" a bullet listing the packet-half files, and to "Broke / learned" the reviewer's most important findings and what you changed. Do not create a new entry and do not touch the milestone table.
Findings: ${JSON.stringify(review.findings)}
Return a short summary.`, { label: 'fix', phase: 'Fix', effort: 'high' })
return { written, review, fixed }

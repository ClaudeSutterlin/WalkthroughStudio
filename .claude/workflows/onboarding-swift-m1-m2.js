export const meta = {
  name: 'onboarding-swift-m1-m2',
  description: 'Write the Swift for onboarding milestones M1 and the non-packet half of M2 (stills writer, selftest scaffold, anchors, package store, git runner), then review by reading since no compiler is available, then fix',
  phases: [
    { title: 'Write', detail: 'three writers, disjoint files' },
    { title: 'Review', detail: 'two read-through reviewers: compile plausibility and API correctness' },
    { title: 'Fix', detail: 'one fixer applies confirmed findings' },
  ],
}

const COMMON = `
CONTEXT. Repository: Walkthrough Studio, a macOS SwiftUI Swift Package at the current working directory (macOS 14 floor, Swift 5.10 tools, no third-party Swift dependencies). We are building the "Onboard to a Codebase" feature. The plan is docs/onboarding/ARCHITECTURE.md (read sections 1, 2.2 to 2.4, 3, 9, 15 and 15.6) and the build log is docs/onboarding/BUILD-LOG.md. THERE IS NO SWIFT COMPILER IN THIS ENVIRONMENT: nobody can build or run what you write here, so write conservative, plain Swift that you are confident compiles on Xcode 26 / macOS 14+ APIs, mirror the style and helpers of the existing code (read SelfTest.swift, WalkthroughStudioApp.swift, Services/VideoService.swift, Services/Exporters.swift, Models.swift, scripts/make-test-video.swift first), avoid clever generics, and prefer Foundation and AVFoundation APIs you have seen in this codebase. Every new file starts with a short header comment saying what it is and which milestone it belongs to. Do not touch files owned by another writer (listed below). Do not run git commands that modify the repository.

PROBE CONTRACT (shared by all writers). Probes live in "extension SelfTest" and have the exact signature
    static func <name>(_ ctx: OnboardingProbeContext) async throws
where OnboardingProbeContext is defined ONCE in Sources/WalkthroughStudio/SelfTestOnboarding.swift (writer A) as
    struct OnboardingProbeContext { let fixtureRepo: URL; let outDir: URL; let scratch: URL }
(scratch is a fresh temp directory the runner creates per run). A probe prints "selftest: <name> OK (<detail>)" on success and throws StudioError("<name>: <observed value>") on failure (StudioError exists in Models.swift; check its initializer). The runner in SelfTestOnboarding.swift calls, in this order: fixtureRepoProbe, stillsWriterProbe, anchorRoundTripProbe, manifestRoundTripProbe, packageStoreProbe, gitRunnerProbe. Writers B and C implement their probes in their own files with exactly those names.

FIXTURE REPOSITORY FACTS (scripts/make-fixture-repo.sh <dir> builds it; it is deterministic): head SHA fb63e78744ce026d810547afbb62c0a08ce2040a; 12 commits; authors Ada Lovelace (8) and Grace Hopper (4); files include src/api/orders_handler.py, src/auth/authz.py, src/service/orders.py, src/repo/orders_repo.py (touched in 6 commits), db/schema.sql (contains "email"), db/migrations/001_orders.sql and 002_split_addresses.sql, .github/workflows/ci.yml, deploy/deploy.sh (untouched since commit 1, dated 2025-01-04), requirements.txt (requests==2.19.0), tests/test_orders.sh (prints PASS), config/settings.example (FAKE_SECRET=sk-test-fixture-0001), templates/email.tmpl (contains {{ user.name }}), vendor/generated/client_pb2.py. The selftest may shell out to /usr/bin/git via Process.
`

const WRITE_SCHEMA = { type: 'object', properties: {
  filesWritten: { type: 'array', items: { type: 'string' } },
  filesEdited: { type: 'array', items: { type: 'string' } },
  publicAPI: { type: 'string', description: 'the types and functions other writers or later milestones will call, with signatures' },
  uncertainties: { type: 'array', items: { type: 'string' }, description: 'API usages you are not fully sure compile; the reviewers check these first' },
}, required: ['filesWritten', 'filesEdited', 'publicAPI', 'uncertainties'] }

phase('Write')
const writers = [
  { key: 'A-selftest-stills', prompt: `${COMMON}
YOU ARE WRITER A. You own: Sources/WalkthroughStudio/Services/StillsVideoWriter.swift (new), Sources/WalkthroughStudio/SelfTestSupport.swift (new), Sources/WalkthroughStudio/SelfTestOnboarding.swift (new), and edits to Sources/WalkthroughStudio/WalkthroughStudioApp.swift and (only if needed to make a helper internal) Sources/WalkthroughStudio/SelfTest.swift.
1. StillsVideoWriter: "static func write(frames: [(image: CGImage, hold: Double)], size: CGSize, fps: Int = 30, to url: URL) async throws" ported from scripts/make-test-video.swift (AVAssetWriter, H.264, .mp4, 600 timescale). Append each frame at its shot start and then every 0.5 s of its hold, plus one final frame 1.0 s after the last hold so the asset is never shorter than the segment sum (ARCHITECTURE section 5 step 5). Handle pixel buffer creation from CGImage (BGRA), requestMediaDataWhenReady or a simple synchronous append loop with isReadyForMoreMediaData polling, finishWriting with a continuation. Also add "static func solidFrame(color: (r: CGFloat, g: CGFloat, b: CGFloat), size: CGSize) -> CGImage" for probes.
2. SelfTestSupport: internal helpers used by onboarding probes: pixel(in: CGImage, x:, y:) -> (r,g,b) using an RGBA CGBitmapContext with the top-row-first convention DOCUMENTED in a comment (CLAUDE.md landmine 6), "sineWAV(seconds:) -> Data" (if SelfTest.swift already has a private sineWAV, make that one internal and call it instead of duplicating; read the file), and "func runGit(_ args: [String], in dir: URL) throws -> String" (Process, /usr/bin/git, captures stdout, throws StudioError with stderr on non-zero). Do NOT refactor the five NSHostingView harnesses in SelfTest.swift now; leave a TODO comment naming them.
3. SelfTestOnboarding.swift: define OnboardingProbeContext; "extension SelfTest { static func runOnboarding(fixtureRepo: URL, outDir: URL, only: String?) async throws }" which creates outDir and a scratch dir, builds the probe list [(name, closure)] for the six names in the contract, runs all or the one named by --probe, prints progress, and rethrows. Implement fixtureRepoProbe (git rev-parse HEAD equals the fixture SHA, rev-list --count HEAD == 12, shortlog counts 8 and 4, the listed files exist, db/schema.sql contains "email", running tests/test_orders.sh exits 0 and prints PASS) and stillsWriterProbe (three solid frames red/green/blue with holds 2, 3, 4 s at 1920x1080 -> outDir/stills-probe.mp4; duration within 0.1 s of 10.0 s using AVURLAsset load(.duration); VideoService.extractFrame at 1.0, 4.25, 8.0 s and at 9.9 s returns the expected dominant color via SelfTestSupport.pixel; then VideoService.assembleNarratedVideo over it with a sineWAV narration for segment 0 and the existing ExportSegment shape, asserting the output has an audio track whose time range end is past the last segment start; read VideoService.swift to get the exact signatures).
4. WalkthroughStudioApp.swift: add "--selftest-onboarding <fixtureRepo> <outDir> [--probe <name>]" parsing beside --selftest with the same print/exit pattern, and change applicationShouldTerminateAfterLastWindowClosed to return false when ANY argument hasPrefix("--selftest") (CLAUDE.md landmine 8). Keep the existing --selftest behavior byte-for-byte.
Return the structured summary.` },
  { key: 'B-anchor-package', prompt: `${COMMON}
YOU ARE WRITER B. You own new files under Sources/WalkthroughStudio/Onboarding/Package/: Anchor.swift, OnboardingModels.swift, PackageStore.swift, BuildLog.swift, and Sources/WalkthroughStudio/SelfTestOnboarding+Package.swift (probes). Do not touch SelfTestOnboarding.swift, WalkthroughStudioApp.swift, or anything under Services/.
1. Anchor.swift: "enum Anchor: Hashable, Codable" with cases code(path: String, sha7: String?, lines: ClosedRange<Int>?) [a trailing "/" on path means directory], video(id: String, seconds: Double?, chapter: String?), doc(id: String, slug: String), diagram(id: String, node: String), trace(id: String, hop: Int), fact(id: String), commit(sha: String), cmd(unit: String, n: Int), issue(n: Int), url(String). "init?(string:)" parses the grammar in ARCHITECTURE 2.2 and docs/onboarding/PACKET.md section 3 EXACTLY: for code: the path is everything before the LAST "@" followed by 7 to 40 hex chars, the fragment is after the first "#" after that ("#L12" or "#L12-L30"); a code anchor without @sha is accepted with sha7 nil; directory anchors reject fragments; reject "..", leading "/", empty path, end before start. "var string: String" round-trips. "var url: URL" gives the walkthrough:// form from 2.2. Codable as a single string (encode string, decode via init?(string:) else throw). Add "static func parse(_:) throws -> Anchor" that throws StudioError with the reason.
2. OnboardingModels.swift: Codable structs from ARCHITECTURE 2.3 and 2.4 with EXPLICIT encode(to:) and init(from:) (the BrandTheme lesson in CLAUDE.md: a decode-only legacy key breaks synthesis; unknown keys must be ignored; a missing optional decodes as nil): OnboardingManifest (version, repoURL, localClone, headSHA, defaultBranch, createdAt, scope, models{planner, worker, override}, narration{voiceID, modelID, timingSource}, budget{capUSD optional}, spent{...}, status enum, unproduced, coverage{unreadDirs, skippedChecks}, deliverables [Deliverable], edges [Edge], producer{name, version, model, finishedAt} optional, briefingPath optional), WorkPlan and WorkUnit (2.4), OnboardingNotice {id, message, retryUnitId?}, OnboardingStage enum (acquire, survey, research, verify, synthesize, project, render, hub, complete). Keep Date as ISO8601 via JSONEncoder/Decoder strategies set in a shared "OnboardingJSON" enum with static encoder()/decoder() (sortedKeys, prettyPrinted, iso8601).
3. PackageStore.swift: "final class PackageStore" (not an actor; called from async code on MainActor or background, keep it simple and thread-confined) with init(root: URL) creating the layout from ARCHITECTURE 2.1 (packet/, repo/, dossier/, facts/, units/, diagrams/, docs/, traces/, videos/, index/, hub/, review/, chat/), "func writeAtomically(_ data: Data, to relativePath: String) throws" (write to a temp file in the same directory then replaceItemAt/rename), "func readManifest() throws -> OnboardingManifest", "func writeManifest(_:)", "func readWorkPlan()/writeWorkPlan(_:)", "func sha256(of data: Data) -> String" (CryptoKit), "static func packageURL(outputDir: URL, repoName: String) -> URL" giving <outputDir>/<RepoName>.onboarding.
4. BuildLog.swift: "struct BuildLog" bound to a PackageStore: "func appendMarkdown(_ line: String)" appends to build-log.md, "func appendRecord(_ record: BuildLogRecord)" appends one JSON line to build-log.jsonl (BuildLogRecord: ts, unitId, model, inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens, usd, stopReason, ms, streaming), "func beginSession(id: UUID)" writes the "## Session <uuid> <ISO date>" header. Never write headers or keys.
5. Probes in SelfTestOnboarding+Package.swift: anchorRoundTripProbe (at least 14 cases including a path containing "@" like "src/pkg@v2/a.swift@fb63e78#L1-L3", a path containing "#", a directory anchor, "#L5" single line, "#c=" and "#t=" video forms, url form both ways, and three malformed strings that must be rejected); manifestRoundTripProbe (encode, decode, and decode a JSON with an unknown extra key and a missing optional); packageStoreProbe (layout created under ctx.scratch, atomic write leaves no .tmp on success, build-log.md and build-log.jsonl grow across two BuildLog instances, sha256 of a known string equals the known digest). Each prints selftest: <name> OK (...).
Return the structured summary.` },
  { key: 'C-git', prompt: `${COMMON}
YOU ARE WRITER C. You own new files Sources/WalkthroughStudio/Onboarding/Package/GitRunner.swift and RepoAcquisition.swift, an additive edit to Sources/WalkthroughStudio/Services/Keychain.swift (a "githubToken" account with the same get/set pattern as the existing keys; read the file), and Sources/WalkthroughStudio/SelfTestOnboarding+Git.swift (gitRunnerProbe). Do not touch other files.
1. GitRunner.swift: "struct GitRunner" that shells out with Process to the git found by "xcrun --find git" (fall back to /usr/bin/git only if xcrun succeeds; if xcrun fails throw StudioError explaining the Command Line Tools are missing; ARCHITECTURE section 3 and 14). All calls run off the main actor (use a detached Task or a serial DispatchQueue with a continuation), with a timeout (default 600 s, kill on expiry), capture stdout and stderr, and throw StudioError with the stderr tail on non-zero exit. Functions: revParse(_ ref: String, in repo: URL) -> String; isShallow(repo:) -> Bool; isDirty(repo:) -> Bool (status --porcelain non-empty); show(sha: String, path: String, repo: URL) -> String; lineCount(sha:path:repo:) -> Int; log(path: String?, limit: Int, repo: URL) -> [GitCommit] (sha, author, dateISO, subject; use --format with %x1f separators); blame(path:startLine:endLine:repo:) -> [GitBlameLine] (line, author, sha, dateISO via --line-porcelain); lsTree(sha:path:repo:) -> [String]; clone(url: String, to dir: URL, token: String?) (pass "-c credential.helper=" and, when a token is given, a GIT_ASKPASS helper script written to a private temp dir that echoes an environment variable GIT_TOKEN set only in the child's environment; never write the token to disk or logs; delete the helper afterwards); archive(sha: String, from repo: URL, into dir: URL) (git archive <sha> | tar -x into dir; implement as two Processes with a Pipe); worktreeList(repo:) -> [String].
2. RepoAcquisition.swift: "enum RepoSource { case url(String), local(URL) }" and "struct RepoAcquisition" with "static func acquire(_ source: RepoSource, into store: PackageStore, pinnedSHA: String?, token: String?, progress: @escaping (String) -> Void) async throws -> AcquiredRepo" where AcquiredRepo { headSHA, localClone: URL?, repoDir: URL, notices: [String] }. For .url: clone into <package>/repo-git, resolve the sha (pinned or HEAD), archive into <package>/repo. For .local: never touch the user's repository; check isDirty and isShallow and add notices ("Uncommitted changes are not part of the package", "Shallow clone: history mining is limited; run git fetch --unshallow"); archive HEAD (or pinned) into <package>/repo. PackageStore is written by writer B: assume "final class PackageStore { let root: URL; init(root: URL) throws }" and only use store.root to build paths; do not call other PackageStore methods.
3. gitRunnerProbe: on ctx.fixtureRepo: revParse("HEAD") equals the fixture SHA; isShallow false; isDirty false; lineCount of src/repo/orders_repo.py at HEAD is > 20 and equals the number of lines of show(); log(limit: 100) returns 12 commits with author counts Ada 8 and Grace 4; blame of db/schema.sql lines 1-3 names Ada Lovelace; archive HEAD into ctx.scratch/archive then compare file lists with lsTree; worktreeList of the fixture is unchanged (exactly one entry) after archive; RepoAcquisition.acquire(.local(fixture)) into a PackageStore at ctx.scratch/pkg yields repoDir containing db/schema.sql and zero notices. Print selftest: gitRunnerProbe OK (...).
Return the structured summary.` },
]
const written = (await parallel(writers.map(w => () => agent(w.prompt, { label: `write:${w.key}`, phase: 'Write', schema: WRITE_SCHEMA, effort: 'high' })))).filter(Boolean)
log(`writers done: ${written.length}/3`)
const digest = JSON.stringify(written)

const REVIEW_SCHEMA = { type: 'object', properties: {
  findings: { type: 'array', items: { type: 'object', properties: {
    file: { type: 'string' }, line: { type: 'integer' }, severity: { type: 'string', enum: ['compile-error', 'runtime-bug', 'contract-mismatch', 'style'] },
    problem: { type: 'string' }, fix: { type: 'string' } }, required: ['file', 'line', 'severity', 'problem', 'fix'] } },
  verdict: { type: 'string' },
}, required: ['findings', 'verdict'] }

phase('Review')
const lenses = [
  { key: 'compile', brief: 'You are the compiler. Read every new and edited Swift file (git status and git diff show them) and every existing file they reference. Find anything that will not compile on Swift 5.10 / macOS 14 SDK: wrong initializer labels, non-existent APIs, missing imports (AVFoundation, CoreGraphics, CryptoKit, AppKit), actor isolation errors (calling MainActor APIs from nonisolated code and vice versa; SelfTest.run is @MainActor context), Sendable and concurrency warnings that are errors under strict settings, optional misuse, Codable synthesis conflicts with explicit encode/init, duplicate symbol names across files (e.g. two definitions of sineWAV, pixel, OnboardingProbeContext), access control (private helpers used from another file), unused-result warnings that break under -warnings-as-errors (check Package.swift for settings). Report each with the exact fix.' },
  { key: 'api', brief: 'You are the API and behavior reviewer. Check: AVAssetWriter usage (pixel buffer format, presentation times on the 600 timescale, final frame appended after the last hold, finishWriting awaited properly, mp4 with H.264 settings identical in spirit to scripts/make-test-video.swift); VideoService.assembleNarratedVideo and extractFrame called with their real signatures (read VideoService.swift); Process usage (arguments, environment, pipes read before waitUntilExit to avoid deadlock on large output, timeouts, xcrun --find git); the GIT_ASKPASS helper never persists the token; atomic writes (temp file in the same directory then rename or replaceItemAt); Anchor parsing matches PACKET.md section 3 exactly including the last-@ rule and directory anchors; Codable explicit encode/init handle unknown keys and missing optionals; the probe contract (names, signature, print format, StudioError on failure) is met by every probe and the runner lists exactly the six names; the --selftest-onboarding flag and the hasPrefix terminate guard are in WalkthroughStudioApp.swift and the existing --selftest path is unchanged. Report each problem with the exact fix.' },
]
const reviews = (await parallel(lenses.map(l => () => agent(`${COMMON}
YOU ARE A REVIEWER. ${l.brief}
Writer summaries (their own uncertainties come first in your checklist): ${digest}
Read-only. Return the structured findings (empty list is acceptable only if you actually read everything).`, { label: `review:${l.key}`, phase: 'Review', schema: REVIEW_SCHEMA, effort: 'high' })))).filter(Boolean)
const findings = reviews.flatMap(r => r.findings)
log(`review findings: ${findings.length}`)

phase('Fix')
const fixed = await agent(`${COMMON}
YOU ARE THE FIXER. Two reviewers read the new Swift and reported findings below. Apply every compile-error, runtime-bug and contract-mismatch finding (verify each against the file first; if a finding is wrong, say so and skip it). Apply style findings only when trivial. After fixing, re-read all new files once more as a compiler would and fix anything else you see. Then update docs/onboarding/BUILD-LOG.md: entry "### S2:" ALREADY EXISTS (the orchestrator wrote it after a usage-limit interruption). Do NOT create a new entry and do not touch the milestone table or S1. Under S2's "Broke / learned" section append bullets with the reviewers' most important findings and exactly what you changed in response (file and symbol names). Leave "Verified" saying not verified.
Findings: ${JSON.stringify(findings)}
Return a short summary of what you changed and what you skipped and why.`, { label: 'fix', phase: 'Fix', effort: 'high' })

return { written, findings, fixed }
{
  "version": 1,
  "headSHA": "fb63e78744ce026d810547afbb62c0a08ce2040a",
  "terms": [
    {
      "term": "applied (migration header)",
      "definition": "A first-line SQL comment ('-- applied' or '-- NOT yet applied') that is the only record of whether a db/migrations/ file has been run; it is one flag for all environments and is flipped by hand.",
      "definedAt": "code:db/migrations/001_orders.sql@fb63e78#L1",
      "factIds": []
    },
    {
      "term": "schema.sql",
      "definition": "The hand-maintained bootstrap schema for a fresh 'orders' database; by convention applied migrations are appended to it verbatim under a '-- NNN_name.sql' comment, so it already contains 001 and running the migrations folder after it fails.",
      "definedAt": "code:db/schema.sql@fb63e78#L13-L14",
      "factIds": []
    },
    {
      "term": "address split",
      "definition": "The planned but unapplied migration 002 that moves users.address into a separate addresses table (line1, city, country) and drops the column, with no data copy step.",
      "definedAt": "code:db/migrations/002_split_addresses.sql@fb63e78#L2-L7",
      "factIds": []
    },
    {
      "term": "pin",
      "definition": "An exact '==' version constraint in requirements.txt; this repository pins its two direct dependencies (requests 2.19.0, psycopg2 2.8.6) but nothing transitive, and no pipeline installs the file.",
      "definedAt": "code:requirements.txt@fb63e78#L1-L2",
      "factIds": []
    },
    {
      "term": "psycopg2 vs psycopg2-binary",
      "definition": "psycopg2 is the source distribution that compiles against the host's libpq headers; psycopg2-binary ships prebuilt wheels. The manifest pins the source form, so installs need Postgres development headers.",
      "definedAt": "code:requirements.txt@fb63e78#L2",
      "factIds": []
    },
    {
      "term": "orders (systemd unit)",
      "definition": "The systemd service named 'orders' that deploy.sh restarts over ssh; its unit file (interpreter, working directory, environment) is not in the repository and is the only thing that decides how the Python code is actually run on a host.",
      "definedAt": "code:deploy/deploy.sh@fb63e78#L6",
      "factIds": []
    },
    {
      "term": "/srv/orders",
      "definition": "The install directory on every host into which deploy.sh scp's the src/ tree; the process must run with this directory on sys.path so that 'from src.auth.authz import ...' resolves.",
      "definedAt": "code:deploy/deploy.sh@fb63e78#L5",
      "factIds": []
    },
    {
      "term": "deploy user",
      "definition": "The 'deploy' Unix account on each <env>.example.com host that receives scp uploads and runs 'sudo systemctl restart orders'; its keys and sudo rules live outside the repository.",
      "definedAt": "code:deploy/deploy.sh@fb63e78#L5-L6",
      "factIds": []
    },
    {
      "term": "order",
      "definition": "One row of the orders table (id, user_id, total, created_at), created by OrderService.create from a user_id and a list of items; returned as {id, total} on creation and {id, user_id, total} on fetch. Line items are not part of an order as stored.",
      "definedAt": "code:db/schema.sql@fb63e78#L7-L14",
      "factIds": []
    },
    {
      "term": "items",
      "definition": "The request-body list of {price, qty} dicts passed to create; summed into total and then discarded, never persisted. There is no line-item table.",
      "definedAt": "code:src/service/orders.py@fb63e78#L10",
      "factIds": []
    },
    {
      "term": "total",
      "definition": "The order amount: sum(price*qty) over items, computed as a Python float, stored in orders.total as NUMERIC(10,2), read back as decimal.Decimal. No currency is recorded.",
      "definedAt": "code:src/service/orders.py@fb63e78#L10",
      "factIds": []
    },
    {
      "term": "user",
      "definition": "A row of the users table (id SERIAL, email TEXT NOT NULL -- PII, address TEXT). Application code never reads or writes users; a user exists to the service only as the integer user_id on orders and in the X-User header. No code path creates users.",
      "definedAt": "code:db/schema.sql@fb63e78#L1-L5",
      "factIds": []
    },
    {
      "term": "address",
      "definition": "Today: the nullable free-text users.address column. Planned (migration 002, not applied): a separate addresses table (id, user_id, line1, city, country) with users.address dropped. Nothing in src/ reads either, despite the schema TODO claiming otherwise.",
      "definedAt": "code:db/schema.sql@fb63e78#L4",
      "factIds": []
    },
    {
      "term": "addresses (table)",
      "definition": "The planned per-user postal address table introduced by migration 002; does not exist in any applied schema at HEAD.",
      "definedAt": "code:db/migrations/002_split_addresses.sql@fb63e78#L2-L6",
      "factIds": []
    },
    {
      "term": "user_id",
      "definition": "Integer identifying a user; arrives in the JSON body, is compared as str() against the X-User header, and is written to orders.user_id (FK to users.id). The only tenant/ownership signal in the system.",
      "definedAt": "code:src/api/orders_handler.py@fb63e78#L8-L9",
      "factIds": []
    },
    {
      "term": "X-User",
      "definition": "Request header carrying the caller's user id as a string; the sole identity signal. require_user passes iff header == str(body user_id). Not authenticated.",
      "definedAt": "code:src/auth/authz.py@fb63e78#L9",
      "factIds": []
    },
    {
      "term": "Forbidden / 'user mismatch'",
      "definition": "The bare Exception raised by require_user when X-User does not equal str(user_id). Carries no HTTP status; the hosting framework must translate it.",
      "definedAt": "code:src/auth/authz.py@fb63e78#L4-L10",
      "factIds": []
    },
    {
      "term": "idempotency key",
      "definition": "A client-supplied token that would let a retried order INSERT be recognised and not re-applied. Referenced as missing in the handler TODO and the repo comment; exists nowhere in the schema or code. Its absence is the root cause of the production duplicate-order incident.",
      "definedAt": "code:src/api/orders_handler.py@fb63e78#L7",
      "factIds": []
    },
    {
      "term": "attempt (retry)",
      "definition": "One iteration of OrdersRepo.insert's `for attempt in range(2)` loop: two attempts total, one retry, sleeping 0.1s then 0.2s after an OperationalError. The count was 3 and was reverted to 2 after duplicate orders in production.",
      "definedAt": "code:src/repo/orders_repo.py@fb63e78#L13-L17",
      "factIds": []
    },
    {
      "term": "double insert / duplicate order",
      "definition": "The failure where an OperationalError after a successful commit causes the retry loop to run the INSERT again, creating two orders rows for one request. Observed in production at 3 attempts (commit 27caac5).",
      "definedAt": "code:src/repo/orders_repo.py@fb63e78#L12",
      "factIds": []
    },
    {
      "term": "orders (database)",
      "definition": "The literal Postgres database name hard-coded in psycopg2.connect(\"dbname=orders\"); host, port, user and password come from libpq PG* environment variables, not from DATABASE_URL.",
      "definedAt": "code:src/repo/orders_repo.py@fb63e78#L9",
      "factIds": []
    },
    {
      "term": "migration",
      "definition": "A numbered raw SQL file in db/migrations/ applied by hand with psql; there is no runner and no tracking table. Migration 001's ALTER is also inlined in schema.sql, so bootstrap-then-migrate fails on 001.",
      "definedAt": "code:db/migrations/001_orders.sql@fb63e78#L1-L2",
      "factIds": []
    },
    {
      "term": "applied / NOT yet applied",
      "definition": "The first-line comment in each migration file recording its status. It is one flag per repository, not per environment database, so it cannot say which env has which migration.",
      "definedAt": "code:db/migrations/002_split_addresses.sql@fb63e78#L1",
      "factIds": []
    },
    {
      "term": "created_at",
      "definition": "orders.created_at TIMESTAMP DEFAULT now(), added by migration 001; populated by the database but never selected or returned by any repo method.",
      "definedAt": "code:db/schema.sql@fb63e78#L13-L14",
      "factIds": []
    },
    {
      "term": "env",
      "definition": "The single positional argument to deploy.sh; used only as the subdomain of the target host (deploy@<env>.example.com). It selects no config file or database.",
      "definedAt": "code:deploy/deploy.sh@fb63e78#L4-L5",
      "factIds": []
    },
    {
      "term": "deploy",
      "definition": "`deploy/deploy.sh <env>`: scp -r of the local src/ working tree to /srv/orders/ on the host, then `sudo systemctl restart orders`. No rollback, no migrations, no requirements install; recovery is redeploying the previous commit.",
      "definedAt": "code:deploy/deploy.sh@fb63e78#L2-L6",
      "factIds": []
    },
    {
      "term": "smoke test",
      "definition": "tests/test_orders.sh: an inline Python heredoc asserting that a hard-coded two-item list sums to 6.0, then `echo PASS`. It imports no project module despite its comment, and is the whole CI test step.",
      "definedAt": "code:tests/test_orders.sh@fb63e78#L2-L8",
      "factIds": []
    },
    {
      "term": "PII",
      "definition": "Personally identifiable information; in this schema only users.email is annotated as such (comment), though users.address is also personal data. Stored in plaintext.",
      "definedAt": "code:db/schema.sql@fb63e78#L3",
      "factIds": []
    },
    {
      "term": "generated client (client_pb2)",
      "definition": "vendor/generated/client_pb2.py, marked 'Generated by protoc. DO NOT EDIT'. No .proto source, no codegen step and no importer exist in the repository; it is vendored, unreferenced and not regenerable from here.",
      "definedAt": "code:vendor/generated/client_pb2.py@fb63e78#L1-L2",
      "factIds": []
    },
    {
      "term": "confirmation email",
      "definition": "templates/email.tmpl, a plain-text body with {{ user.name }}, {{ order.id }} and {{ order.total }} placeholders. No engine is declared and no code renders or sends it; user.name has no backing column.",
      "definedAt": "code:templates/email.tmpl@fb63e78#L1-L3",
      "factIds": []
    }
  ]
}

{
  "pathId": "order-creation",
  "title": "Create an order (POST create-order → OrderService.create → OrdersRepo.insert)",
  "entry": "code:src/api/orders_handler.py@fb63e78#L6-L11",
  "scenario": "A client POSTs an order for user 42: headers {\"X-User\": \"42\"}, body {\"user_id\": 42, \"items\": [{\"price\": 2.5, \"qty\": 2}, {\"price\": 1.0, \"qty\": 1}]}. handle_create_order reads user_id 42, require_user compares the X-User header \"42\" to str(42) and passes, OrderService.create folds the two items into total = 6.0, and OrdersRepo.insert writes one row into orders and returns {\"id\": 1001, \"total\": 6.0}. The interesting variant, followed at hop 6, is the same request when psycopg2 raises OperationalError on the commit round-trip after the row is already durable: the loop retries and a second order row for user 42 is created.",
  "hops": [
    {
      "n": 1,
      "anchor": "code:src/api/orders_handler.py@fb63e78#L6-L11",
      "summary": "handle_create_order pulls user_id straight out of request.json, calls require_user, constructs a fresh OrderService per request, and returns whatever the service returns — its own TODO on L7 admits the repo's retries can double-insert.",
      "factIds": [
        "F-map-src-002",
        "F-map-src-005",
        "F-map-src-006",
        "F-map-src-007",
        "F-map-src-008",
        "F-map-src-039",
        "F-map-src-040"
      ],
      "callSite": null
    },
    {
      "n": 2,
      "anchor": "code:src/auth/authz.py@fb63e78#L8-L10",
      "callSite": "code:src/api/orders_handler.py@fb63e78#L9",
      "summary": "require_user raises Forbidden unless the X-User header string-equals str(user_id), which compares two values the same caller supplied and so proves only that the header and the body agree, never who the caller is.",
      "factIds": [
        "F-map-src-011",
        "F-map-src-012",
        "F-map-src-013",
        "F-map-src-014",
        "F-map-src-015",
        "F-lens-security-tests-001",
        "F-lens-security-tests-005",
        "F-lens-security-tests-006",
        "F-lens-deploy-ops-011"
      ]
    },
    {
      "n": 3,
      "anchor": "code:src/service/orders.py@fb63e78#L5-L7",
      "callSite": "code:src/api/orders_handler.py@fb63e78#L10",
      "summary": "OrderService.__init__ hard-constructs an OrdersRepo, so there is no injection seam between business logic and the database on this request.",
      "factIds": [
        "F-map-src-007",
        "F-lens-landmines-glossary-003",
        "F-lens-security-tests-013"
      ]
    },
    {
      "n": 4,
      "anchor": "code:src/repo/orders_repo.py@fb63e78#L7-L9",
      "callSite": "code:src/service/orders.py@fb63e78#L7",
      "summary": "OrdersRepo.__init__ opens a new psycopg2 connection to the hard-coded DSN \"dbname=orders\" with no connect_timeout, outside any retry, and the module docstring on L1 states the connection is never closed.",
      "factIds": [
        "F-map-src-007",
        "F-map-src-025",
        "F-map-src-026",
        "F-lens-deploy-ops-014",
        "F-lens-landmines-glossary-013"
      ]
    },
    {
      "n": 5,
      "anchor": "code:src/service/orders.py@fb63e78#L9-L11",
      "callSite": "code:src/api/orders_handler.py@fb63e78#L11",
      "summary": "OrderService.create is the entire business logic: it folds items into total = sum(price * qty) in Python float arithmetic (2.5*2 + 1.0*1 = 6.0) and passes user_id, items and total to the repo.",
      "factIds": [
        "F-map-src-018",
        "F-map-src-020",
        "F-map-src-021",
        "F-map-db-006",
        "F-lens-data-migrations-007",
        "F-lens-landmines-glossary-015",
        "F-lens-landmines-glossary-016",
        "F-lens-security-tests-011"
      ]
    },
    {
      "n": 6,
      "anchor": "code:src/repo/orders_repo.py@fb63e78#L11-L18",
      "callSite": "code:src/service/orders.py@fb63e78#L11",
      "summary": "OrdersRepo.insert calls _insert_once up to twice, catching only psycopg2.OperationalError and sleeping 0.1s then 0.2s between attempts, with no idempotency key — the literal 2 on L13 is an incident-tuned constant reverted from 3 after duplicate orders in production.",
      "factIds": [
        "F-map-src-027",
        "F-map-src-028",
        "F-map-src-029",
        "F-map-src-030",
        "F-map-src-034",
        "F-lens-data-migrations-004",
        "F-lens-deploy-ops-010",
        "F-lens-deploy-ops-012",
        "F-lens-deploy-ops-015",
        "F-lens-deploy-ops-017",
        "F-lens-landmines-glossary-001",
        "F-lens-landmines-glossary-002"
      ]
    },
    {
      "n": 7,
      "anchor": "code:src/repo/orders_repo.py@fb63e78#L20-L25",
      "callSite": "code:src/repo/orders_repo.py@fb63e78#L15",
      "summary": "_insert_once executes a parameterized INSERT INTO orders (user_id, total) ... RETURNING id, reads the new id, commits, and returns {\"id\": order_id, \"total\": total} — the items list never reaches SQL.",
      "factIds": [
        "F-map-src-028",
        "F-map-src-035",
        "F-map-src-037",
        "F-map-src-038",
        "F-map-db-015",
        "F-map-db-020"
      ]
    },
    {
      "n": 8,
      "anchor": "code:db/schema.sql@fb63e78#L7-L11",
      "callSite": "code:src/repo/orders_repo.py@fb63e78#L22",
      "summary": "The write lands in orders (id SERIAL, user_id INTEGER REFERENCES users(id), total NUMERIC(10,2) NOT NULL): no unique or idempotency constraint exists to stop a duplicate, no line-item table exists to hold the items, and the nullable FK points at a users table nothing in this repository ever writes.",
      "factIds": [
        "F-map-db-005",
        "F-map-db-006",
        "F-map-db-015",
        "F-map-db-020",
        "F-lens-data-migrations-003",
        "F-lens-data-migrations-008",
        "F-lens-landmines-glossary-010"
      ]
    },
    {
      "n": 9,
      "anchor": "code:src/api/orders_handler.py@fb63e78#L11",
      "callSite": "code:src/service/orders.py@fb63e78#L11",
      "summary": "The repo's raw dict is returned unchanged through OrderService.create and out of the handler — no serialization, status code or error mapping, so the response shape is whatever psycopg2 and Python produced.",
      "factIds": [
        "F-map-src-002",
        "F-map-src-012",
        "F-map-src-018",
        "F-lens-data-migrations-007"
      ]
    }
  ],
  "concerns": {
    "entry": {
      "status": "present",
      "evidence": [
        "code:src/api/orders_handler.py@fb63e78#L6-L11",
        "code:src/api/orders_handler.py@fb63e78#L1-L3"
      ],
      "note": "handle_create_order(request) is the whole entry and reads request.json directly. Nothing in the repository binds it to an HTTP verb or path: there is no framework in requirements.txt, no router and no packaging, so 'POST create-order' is the function's intent, not routed code."
    },
    "authorization": {
      "status": "present",
      "evidence": [
        "code:src/auth/authz.py@fb63e78#L8-L10",
        "code:src/api/orders_handler.py@fb63e78#L9"
      ],
      "note": "Present but self-referential: require_user compares the X-User header with str(request.json['user_id']), two values the same client sends. It fails closed when the header is missing (None never equals a string), and it catches a body/header mismatch, but it authenticates nobody — anyone who can set a header can be any user."
    },
    "validation": {
      "status": "absent",
      "evidence": [
        "code:src/api/orders_handler.py@fb63e78#L6-L11",
        "code:src/service/orders.py@fb63e78#L9-L11",
        "code:db/schema.sql@fb63e78#L7-L11"
      ],
      "note": "No schema, type or range check anywhere on the path. The handler indexes request.json['user_id'] and ['items'] directly (a missing key is an uncaught KeyError), the service multiplies item['price'] * item['qty'] on whatever JSON supplied, an empty items list yields total 0, and the only validator in the system is PostgreSQL: the users FK, NUMERIC(10,2) and NOT NULL."
    },
    "businessLogic": {
      "status": "present",
      "evidence": [
        "code:src/service/orders.py@fb63e78#L9-L11"
      ],
      "note": "Two lines: total = sum(price * qty) over items, then repo.insert. The items are passed to insert and then dropped — insert forwards only user_id and total to _insert_once — so the composition of an order is computed and discarded."
    },
    "persistence": {
      "status": "present",
      "evidence": [
        "code:src/repo/orders_repo.py@fb63e78#L20-L25",
        "code:db/schema.sql@fb63e78#L7-L11"
      ],
      "note": "One parameterized INSERT ... RETURNING id followed by conn.commit() on a connection opened in OrdersRepo.__init__ and never closed. One row per request in orders; no transaction spans more than the single statement."
    },
    "sideEffects": {
      "status": "absent",
      "evidence": [
        "code:src/service/orders.py@fb63e78#L9-L11",
        "code:src/repo/orders_repo.py@fb63e78#L11-L25",
        "code:templates/email.tmpl@fb63e78#L1-L3"
      ],
      "note": "Creating an order emits nothing: no queue, webhook, event or email. templates/email.tmpl is an order-confirmation body ('Your order {{ order.id }} for {{ order.total }} was received') added in its own commit, but a grep of src/ finds no reference to it, no template engine and no SMTP client, so the confirmation the template promises is never sent."
    },
    "failureHandling": {
      "status": "absent",
      "evidence": [
        "code:src/api/orders_handler.py@fb63e78#L6-L11",
        "code:src/repo/orders_repo.py@fb63e78#L14-L18",
        "code:src/auth/authz.py@fb63e78#L4-L5"
      ],
      "note": "The only except clause on the path catches psycopg2.OperationalError to retry. Everything else escapes raw: KeyError from a malformed body, Forbidden (a bare Exception subclass with no status code or serialization), IntegrityError from the user_id FK, DataError from a total above NUMERIC(10,2), and finally RuntimeError('insert failed after retries') with the original exception discarded. The handler has no try/except and nothing maps any of these to a response."
    },
    "idempotency": {
      "status": "absent",
      "evidence": [
        "code:src/api/orders_handler.py@fb63e78#L7",
        "code:src/api/orders_handler.py@fb63e78#L6-L11",
        "code:src/repo/orders_repo.py@fb63e78#L11-L18",
        "code:db/schema.sql@fb63e78#L7-L11"
      ],
      "note": "The defining hole in this path. The handler accepts no idempotency key and its TODO on L7 says so; insert retries _insert_once with no key, so an OperationalError raised after conn.commit() has succeeded (a timeout on the commit round-trip) re-runs the INSERT and books a second order; and orders has no unique constraint to catch it. Production already saw the duplicates, and the fix (commit 27caac5) reverted the attempt count from 3 to 2 rather than adding a key, so the failure mode is still here and merely fires less often."
    },
    "timeoutsRetries": {
      "status": "present",
      "evidence": [
        "code:src/repo/orders_repo.py@fb63e78#L11-L18",
        "code:src/repo/orders_repo.py@fb63e78#L13",
        "code:src/repo/orders_repo.py@fb63e78#L9"
      ],
      "note": "Retries exist, timeouts do not. range(2) is two attempts total (one retry) with time.sleep(0.1 * (attempt + 1)), and the loop sleeps after the final failed attempt too, so a hard-down database costs 0.3s of wall clock and returns nothing useful. The retry reuses self.conn with no rollback or reconnect, and psycopg2.connect on L9 sets no connect_timeout and no statement_timeout and sits outside the loop, so a hung database hangs the request indefinitely before the retry logic is ever reached."
    },
    "logging": {
      "status": "absent",
      "evidence": [
        "code:src/api/orders_handler.py@fb63e78#L6-L11",
        "code:src/service/orders.py@fb63e78#L9-L11",
        "code:src/repo/orders_repo.py@fb63e78#L11-L25"
      ],
      "note": "There is no logging, metric or trace anywhere in src/ — no logging import, no counters, no audit row (orders has no actor or created-by beyond user_id). The except branch on L16-L17 swallows every OperationalError silently, so the retry rate, the one signal that predicts duplicate orders, is not observable at all. Authorization denials are equally silent."
    }
  },
  "scaresMe": [
    "A retry that fires after the commit already succeeded creates a second, fully valid order for the same customer, and nothing anywhere would tell you: there is no idempotency key, no unique constraint on orders, and no log line on the retry. You would find out from a customer complaining about a double charge, or from a SELECT user_id, total, created_at ... GROUP BY HAVING count(*) > 1 run by hand. This already happened in production; the fix changed the retry count from 3 to 2, which makes it rarer, not impossible.",
    "The single authorization check compares the X-User header with the user_id in the same request body. Anyone who can send a header can set both to 7 and create orders as user 7. If a proxy or gateway upstream is supposed to set or strip X-User, that assumption lives outside this repository and is not asserted anywhere in it, so a routing change that stops stripping the header opens the service silently.",
    "Every request opens a new psycopg2 connection in OrdersRepo.__init__ and never closes it. Under any real traffic you exhaust Postgres connections rather than degrade, and the symptom is connect failures on the healthy path — which is also the one call not wrapped in the retry loop and the one with no connect_timeout, so requests hang instead of erroring.",
    "The handler validates nothing. A body missing 'items' raises KeyError, a user_id that does not exist raises IntegrityError from the foreign key, a total above 99,999,999.99 raises DataError, and none of them are caught or mapped to a response — they escape as whatever the (absent) hosting framework does with an unhandled exception, most likely a 500 and a stack trace.",
    "The items a customer ordered are summed into a total and thrown away. There is no order_items table and insert forwards only user_id and total, so after a disputed order you can say what was charged but not what was bought, and no backfill can recover it.",
    "No test touches any of this. tests/test_orders.sh re-implements sum(price * qty) in a heredoc and asserts 6.0; it imports nothing from src/, so CI stays green through a repeat of the duplicate-order incident, a broken authorization check, or a handler that raises on every request.",
    "src/ has exactly one author across all seven of its commits and orders_repo.py is the top hotspot with six of the repository's twelve commits, including the hotfix and the revert. The reasoning behind the constant 2 on L13 exists only in a commit message; anyone who reads that line as an arbitrary default and raises it re-opens the production incident.",
    "templates/email.tmpl promises the customer a confirmation email that no code sends. Someone will eventually notice confirmations are missing and wire the template in — most likely inside the retry loop or right after commit, which is exactly where a retry would send the second email too."
  ],
  "factIds": [
    "F-map-src-002",
    "F-map-src-005",
    "F-map-src-006",
    "F-map-src-007",
    "F-map-src-008",
    "F-map-src-009",
    "F-map-src-011",
    "F-map-src-012",
    "F-map-src-013",
    "F-map-src-014",
    "F-map-src-015",
    "F-map-src-018",
    "F-map-src-021",
    "F-map-src-027",
    "F-map-src-028",
    "F-map-src-029",
    "F-map-src-030",
    "F-map-src-034",
    "F-map-src-035",
    "F-map-src-037",
    "F-map-src-038",
    "F-map-src-039",
    "F-map-db-005",
    "F-map-db-006",
    "F-map-db-015",
    "F-map-db-020",
    "F-map-ci-011",
    "F-map-ci-012",
    "F-lens-data-migrations-003",
    "F-lens-data-migrations-004",
    "F-lens-data-migrations-007",
    "F-lens-data-migrations-008",
    "F-lens-deploy-ops-010",
    "F-lens-deploy-ops-011",
    "F-lens-deploy-ops-012",
    "F-lens-deploy-ops-014",
    "F-lens-deploy-ops-015",
    "F-lens-deploy-ops-017",
    "F-lens-security-tests-001",
    "F-lens-security-tests-004",
    "F-lens-security-tests-005",
    "F-lens-security-tests-006",
    "F-lens-security-tests-007",
    "F-lens-security-tests-011",
    "F-lens-security-tests-013",
    "F-lens-security-tests-014",
    "F-lens-landmines-glossary-001",
    "F-lens-landmines-glossary-002",
    "F-lens-landmines-glossary-003",
    "F-lens-landmines-glossary-010",
    "F-lens-landmines-glossary-013",
    "F-lens-landmines-glossary-014",
    "F-lens-landmines-glossary-015",
    "F-lens-landmines-glossary-016"
  ],
  "version": 1
}
